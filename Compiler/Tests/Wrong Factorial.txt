

routine factorial(n: integer): bool is
    if n /= 1 then
        var a is n * factorial(n - 1);
        return a;
    else
        return 1;
    end;
end;

routine main(): integer is
    var f is factorial(5);
    return f;
end;


